languages = ["English, French, Ruby"]
# array assigned to languages.
# below the each method is used.
# each allows us to do something to each object the Array
# points to.
languages.each do |lang|
	puts "I love " + lang + "!"
end
